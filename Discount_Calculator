class DiscountCalculator:
    def apply_discount(self, price, discount):
        if not (0 <= discount <= 1):
            raise ValueError("Discount must be between 0 and 1 (e.g., 0.2 for 20%)")
        return price * (1 - discount)
